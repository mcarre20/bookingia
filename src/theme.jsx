import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#00695c",
    },
    secondary: {
      main: "#ff6d00",
    },
  },
});

export default theme;
